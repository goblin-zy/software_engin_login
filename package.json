{
  "name": "your-project-name",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mock": "vite --mode development",
    "dev:real": "vite --mode development",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "build:prod": "vite build --mode production",
    "preview": "vite preview",
    "preview:prod": "vite preview --mode production",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages",
    "deploy:prod": "npm run build:prod && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "msw": "^2.0.0",
    "vite": "^7.1.7"
  },
  "msw": {
    "workerDirectory": "public\\road_of_Tarnished"
  }
}